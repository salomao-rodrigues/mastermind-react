import React from 'react';

import Tray from '../../Tray';

const colors = [
  ["yellow", "orange", "red"],
  ["purple", "blue", "green" ]
];

const Solo = () => (
  <Tray colors={ colors }/>
);

export default Solo;
